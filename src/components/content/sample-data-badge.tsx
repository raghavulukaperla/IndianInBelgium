import { Badge } from "@/components/ui/badge";

export function SampleDataBadge() {
  return (
    <Badge variant="secondary" className="text-xs font-normal text-muted-foreground">
      Sample content — pending admin review
    </Badge>
  );
}
